version: '3.8'

services:
  backend:
    image: ghcr.io/amirtalbi/pouilleux-backend:latest
    environment:
      - NODE_ENV=production
      - PORT=3001
      - ADMIN_PASSWORD=pouilleux-admin
    networks:
      - pouilleux-network
    # Ne pas exposer le port directement

  frontend:
    image: ghcr.io/amirtalbi/pouilleux-front:latest
    ports:
      - "80:80"  # Exposer sur le port 80 standard
    depends_on:
      - backend
    networks:
      - pouilleux-network

networks:
  pouilleux-network:
    driver: bridge
